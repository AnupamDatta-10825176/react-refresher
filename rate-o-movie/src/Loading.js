const styleLoading = {
  fontSize: "2.4rem",
  textAlign: "center",
  fontWeight: "bold",
  margin: "4.8rem",
};

const Loading = () => {
  return (
    <div style={styleLoading}>
      <p>Loading...</p>
    </div>
  );
};

export default Loading;
